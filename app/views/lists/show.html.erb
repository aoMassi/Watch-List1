<div class="position-relative">
  <div class="card-category card-large" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(https://www.moviehouse.co.uk/Media/041a7260-2ab1-442d-bb48-d383c0c4ae91.jpg)">
    <h1><%= @list.name %></h1>
  </div>

  <div>
      <%= link_to 'add a movie', new_list_bookmark_path(@list), class: 'btn btn-flat' %>
      <%= link_to 'detete this list', list_path(@list), method: :delete, class: 'btn btn-cancel', data: { confirm: 'Are you sure you want to delete this list?' } %>
      <%= link_to 'back to list', root_path %>
  </div>